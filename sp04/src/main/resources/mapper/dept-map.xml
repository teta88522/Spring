<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.dept.mapper.DeptMapper"> <!-- 패키지를 포함한 경로 -->
	<!-- select tag 사용 id = 메소드명 resultType = 반환 받는 값의 타입 parameterType = 매개변수 
		타입 -->
	<!-- 다건 조회 -->
	<select id="selectDeptAll" parameterType="DeptVO"
		resultType="DeptVO">
		SELECT DEPARTMENT_ID,DEPARTMENT_NAME,MANAGER_ID ,LOCATION_ID
		FROM
		departments
		ORDER BY department_id
		<!-- ; 붙이면 안됨 -->
	</select>
	<!-- 단건 조회 -->
	<select id="selectDept" parameterType="EmpVO"
		resultType="DeptVO">
		SELECT DEPARTMENT_ID,DEPARTMENT_NAME,MANAGER_ID
		,LOCATION_ID
		FROM departments
		WHERE departmen_id = #{departmentId}
	</select>
	<!-- insert,update,delete 는 return 타입 고정 -->
	<!-- 추가하기 -->

	<insert id="insertDept" parameterType="DeptVO">
		<selectKey keyProperty="departmentId" resultType="Integer"
			order="BEFORE">
			SELECT NVL(MAX(department_id),0) + 10 FROM departments
		</selectKey>
		INSERT INTO departments(departmentId
		,DEPARTMENT_NAME,MANAGER_ID
		,LOCATION_ID)
		VALUES( #{departmentId}
		,#{departmentName}
		, #{managerId}
		,#{locationId})
	</insert>
	<!-- 수정하기 -->
	<update id="updateDept" parameterType="DeptVO">
		UPDATE departments
		SET
		department_name = #{departmentName}
		, manager_id = #{managerId}
		,location_id = #{locationId}
		WHERE department_id = #{departmentId}
	</update>
	<!-- 삭제하기 -->
	<delete id="deleteDept" parameterType="int">
		DELETE FROM departments
		WHERE department_id = #{departmentID}
	</delete>
</mapper>
